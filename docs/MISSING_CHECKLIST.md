# 缺漏与落地前检查清单

## A. 接入前必须完成

- [ ] 为每个 bot/agent 固定 `agent_id` 与 `accountId` 映射
- [ ] 生成统一 `event_id` 规则（建议：`<chatId>:<messageId>:<phase>`）
- [ ] 在“发送成功回调”中调用 `speak`，避免“生成了但没发出”也扣欲望
- [ ] 失败重试不重复扣减（依赖 event_id 幂等）
- [ ] 将 `state/dynamics.db` 放到持久目录并做好备份

## B. 观测与审计

- [ ] 增加决策日志（jsonl）：allow/deny、p、desire 前后、原因
- [ ] 增加每小时指标：总发言数、user触发后平均链长、沉默时长
- [ ] 增加异常告警：连续刷屏/长期静默

## C. 调参策略

- [ ] 先只调 `global_pace`，再调 recover/drop
- [ ] 把“低兴趣 drop”作为首要收敛阀门
- [ ] 建立参数 profile（安静/标准/活跃）三档

## D. 安全与边界

- [ ] 关键词兴趣表不包含敏感诱导项
- [ ] 各角色 `avoid` 在系统提示中强约束
- [ ] 管理员参数文件写权限最小化

## E. 运行维护

- [ ] 增加 `state vacuum` 与历史清理脚本
- [ ] 设计热更新参数命令（不重启生效）
- [ ] 文档化回滚流程（参数回滚 + 代码回滚）
